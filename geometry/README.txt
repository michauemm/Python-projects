First Usr inputs number of geometric shapes (i),
for (i) usr inputs numbers (separated by space), each geometric shape must be provided in a separate input line;
if the line contains:
one number - it's circle radius
two numbers - sides of rectangle
three numbers - sides of triangle
four or more numbers - program returns string "Error! Max 3 numbers!"

Output should contain a sum of all results (rounded to 2 decimal places)
