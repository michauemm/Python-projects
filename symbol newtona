import math
n,k = input().split()
n = int(n)
k = int(k)
def symbol_newtona(n,k):
    odejmowanie = n - k
    mnozenie = math.factorial(k) * math.factorial(odejmowanie)
    dzialanie = math.factorial(n) / mnozenie

    print(math.floor(dzialanie))

symbol_newtona(n,k)
